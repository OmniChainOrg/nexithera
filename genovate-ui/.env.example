# Genovate backend API base URL (FastAPI service)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# WebSocket URL — reserved for live updates in Dashboard PR #9
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

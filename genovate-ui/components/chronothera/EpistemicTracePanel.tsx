import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import type { ChronoTheraSimulation } from '@/lib/types/chronothera';
export function EpistemicTracePanel({ simulation }: { simulation?: ChronoTheraSimulation }) { const t=simulation?.epistemic_trace; return <Card><CardHeader><CardTitle className="text-base">E. EpistemicOS trace</CardTitle></CardHeader><CardContent className="space-y-3 text-sm">{t ? <><div><strong>Zone cluster:</strong> {t.zone_cluster}</div><div className="flex flex-wrap gap-1">{t.zones.map(z=><Badge key={z} variant="outline">{z}</Badge>)}</div><div><strong>CXUs:</strong> {t.cxus.map(c=>c.name).join(', ')}</div><div><strong>Swarm consensus:</strong> {t.swarm.id} · {t.swarm.consensus_score} · {t.swarm.mode}</div><div className="grid gap-2 md:grid-cols-2"><code className="rounded bg-muted p-2 text-xs">input {t.provenance.input_hash}</code><code className="rounded bg-muted p-2 text-xs">output {t.provenance.output_hash}</code></div><div><strong>Engine:</strong> {t.provenance.model_engine_version}</div><div><strong>Assumptions:</strong> {t.provenance.assumptions.join(' ')}</div><div><strong>Uncertainty reasons:</strong> {t.provenance.uncertainty_reasons.join(' ')}</div></> : <p className="text-muted-foreground">Trace appears after the EpistemicOS-backed run.</p>}</CardContent></Card> }

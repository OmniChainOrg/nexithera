import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import type { ChronoTheraSimulation } from '@/lib/types/chronothera';
export function SimulationHistoryPanel({ simulations, onSelect }: { simulations: ChronoTheraSimulation[]; onSelect:(s:ChronoTheraSimulation)=>void }) { return <Card><CardHeader><CardTitle className="text-base">G. Simulation history</CardTitle></CardHeader><CardContent className="space-y-2">{simulations.length?simulations.slice(0,8).map(s=><button key={s.id} onClick={()=>onSelect(s)} className="w-full rounded-md border p-2 text-left text-sm hover:bg-accent"><div className="flex justify-between"><span>{s.id}</span><strong>{s.overall_chronothera_score}</strong></div><div className="text-xs text-muted-foreground">{s.input.formulation_objective} · {s.input.route_of_administration}</div></button>):<p className="text-sm text-muted-foreground">No prior simulations loaded.</p>}<Button variant="outline" className="w-full" disabled>Future asset dossier export queue</Button></CardContent></Card> }

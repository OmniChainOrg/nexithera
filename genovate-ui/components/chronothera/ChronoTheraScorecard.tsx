import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import type { ChronoTheraSimulation } from '@/lib/types/chronothera';
export function ChronoTheraScorecard({ simulation }: { simulation?: ChronoTheraSimulation }) { const entries=Object.entries(simulation?.scorecard ?? {}); return <Card><CardHeader><CardTitle className="text-base">D. ChronoThera readiness scorecard {simulation && <span className="text-primary">· {simulation.overall_chronothera_score}</span>}</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-2">{entries.length?entries.map(([k,s])=><div key={k} className="rounded-lg border p-3"><div className="flex justify-between"><strong>{k.replaceAll('_',' ')}</strong><span className="text-lg font-semibold">{s.score}</span></div><p className="text-xs text-muted-foreground">{s.rationale}</p><List title="Assumptions" items={s.assumptions}/><List title="Uncertainty" items={s.uncertainty}/><p className="mt-2 text-xs"><strong>Next-best action:</strong> {s.next_best_step || s.recommendation}</p></div>):<p className="text-sm text-muted-foreground">Score dimensions render after simulation.</p>}</CardContent></Card> }
function List({title,items}:{title:string;items:string[]}){return <div className="mt-2"><div className="text-[11px] font-semibold uppercase text-muted-foreground">{title}</div><ul className="list-disc pl-4 text-xs">{items.map(i=><li key={i}>{i}</li>)}</ul></div>}

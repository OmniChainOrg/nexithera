import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import type { ChronoTheraAssetPreset } from '@/lib/types/chronothera';
export function AssetSelector({ assets, selectedId, onSelect }: { assets: ChronoTheraAssetPreset[]; selectedId?: string; onSelect:(id:string)=>void }) { const asset = assets.find(a=>a.id===selectedId); return <Card><CardHeader><CardTitle className="text-base">A. Asset context</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><Select value={selectedId} onValueChange={onSelect}><SelectTrigger aria-label="Select ChronoThera asset"><SelectValue placeholder="Select portfolio asset" /></SelectTrigger><SelectContent>{assets.map(a=><SelectItem key={a.id} value={a.id}>{a.label}</SelectItem>)}</SelectContent></Select>{asset && <div className="grid gap-2"><Field k="Portfolio category" v={asset.category}/><Field k="Modality" v={asset.modality}/><Field k="Therapeutic area" v={asset.chronothera_focus.join(', ')}/><Field k="Formulation objective" v={asset.formulation_objectives.join(', ')}/><Field k="Development stage" v="Forge-linked formulation planning"/><Field k="Linked Forge dossier" v={asset.dossier_anchor ?? 'Not linked'}/></div>}</CardContent></Card> }
function Field({k,v}:{k:string;v:string}){return <div className="rounded-md bg-muted/40 p-2"><div className="text-[11px] uppercase tracking-wide text-muted-foreground">{k}</div><div>{v}</div></div>}

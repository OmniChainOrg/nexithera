import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import type { ChronoTheraSimulation } from '@/lib/types/chronothera';
export function GuardianReviewPanel({ simulation }: { simulation?: ChronoTheraSimulation }) { const g=simulation?.guardian_review; return <Card><CardHeader><CardTitle className="text-base">F. Guardian review</CardTitle></CardHeader><CardContent className="space-y-2 text-sm">{g ? <><Badge variant={g.required?'destructive':'secondary'}>{g.required?'Required':'Not required'}</Badge><div>Status: {g.status}</div><div>Trigger reasons: {g.reasons.length?g.reasons.join('; '):'No Guardian triggers for current planning thresholds.'}</div><div>Reviewer notes: {g.notes || 'No reviewer notes yet.'}</div><div className="text-xs text-muted-foreground">Decision controls can attach approved / needs-revision / rejected states through the Guardian API.</div></> : <p className="text-muted-foreground">Guardian requirement is evaluated per simulation.</p>}</CardContent></Card> }

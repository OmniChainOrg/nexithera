import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import type { ChronoTheraSimulation } from '@/lib/types/chronothera';
export function ReleaseProfileChart({ simulation }: { simulation?: ChronoTheraSimulation }) { const data = simulation?.release_profile.labels.map((label,i)=>Object.fromEntries([['label',label],...(simulation.release_profile.datasets.map(d=>[d.api,d.cumulative_release[i]]))])) ?? []; return <Card><CardHeader><CardTitle className="text-base">C. Release / exposure decision output</CardTitle></CardHeader><CardContent>{simulation ? <><div className="h-64"><ResponsiveContainer><LineChart data={data}><XAxis dataKey="label"/><YAxis domain={[0,100]}/><Tooltip/>{simulation.release_profile.datasets.map((d,i)=><Line key={d.api} type="monotone" dataKey={d.api} stroke={["#8b5cf6","#22d3ee","#34d399"][i%3]} strokeWidth={2}/>)}</LineChart></ResponsiveContainer></div><p className="text-xs text-muted-foreground">{simulation.release_profile.datasets[0]?.model}: {simulation.release_profile.datasets[0]?.rationale}</p></> : <p className="text-sm text-muted-foreground">Run a simulation to generate backend release data.</p>}</CardContent></Card> }
